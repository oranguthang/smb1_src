MEMORY {
    TAIL: file = %O, start = $BFBF, size = $203B, fill = yes, fillval = $FF;
    VECTORS: file = %O, start = $DFFA, size = $0006, fill = yes, fillval = $FF;
}

SEGMENTS {
    PRG: load = TAIL, type = rw;
    VECTORS: load = VECTORS, type = rw;
}
